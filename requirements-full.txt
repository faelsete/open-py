# Open-PY — Dependências COMPLETAS (all optional extras)
# Instale com: pip install -r requirements-full.txt

# Core (já incluso no requirements.txt)
-r requirements.txt

# Document Tools
fpdf2>=2.7             # PDF creation
PyMuPDF>=1.24          # PDF reading
openpyxl>=3.1          # Excel creation
Pillow>=10.0           # Screenshots

# Browser Automation
playwright>=1.45       # Headless browser

# Voice (STT + TTS)
faster-whisper>=1.0    # Speech-to-Text (CTranslate2)
piper-tts>=1.2         # Text-to-Speech (ONNX local)

# Media
ffmpeg-python>=0.2     # Audio/Video processing
