# ============================================
# Open-PY — Dependências Python
# ============================================

# === CORE (obrigatório — funciona em 1GB RAM) ===
pydantic>=2.5
tomli>=2.0
tomli-w>=1.0
structlog>=24.0
rich>=13.0
typer>=0.12

# Telegram
aiogram>=3.13

# Database
asyncpg>=0.29
pgvector>=0.3

# LLM
litellm>=1.50

# Embeddings (local, gratuito — ~200MB RAM)
sentence-transformers>=3.0

# Web Search (gratuito)
duckduckgo-search>=6.0

# HTTP
aiohttp>=3.10
aiofiles>=24.0

# Scheduler
APScheduler>=3.10

# Utils
python-dotenv>=1.0
psutil>=5.9

# === OPTIONAL — instale conforme seu hardware ===
# Para ativar, rode: pip install open-py[voice] ou pip install open-py[full]

# [tools] — Document & Browser automation (~150MB)
# playwright>=1.45       # Browser automation
# fpdf2>=2.7             # PDF creation
# PyMuPDF>=1.24          # PDF reading
# openpyxl>=3.1          # Excel creation
# Pillow>=10.0           # Screenshots

# [voice] — STT + TTS (~800MB com modelo base)
# faster-whisper>=1.0    # Speech-to-Text (CTranslate2)
# piper-tts>=1.2         # Text-to-Speech (ONNX local)

# [media] — Manipulação de mídia
# ffmpeg-python>=0.2     # Audio/Video processing
